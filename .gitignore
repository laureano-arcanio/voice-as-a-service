__pycache__/
*.pyc
.venv/
venv/
.env
storage/
.mypy_cache/

# Claude Code: permisos y ajustes locales de cada usuario (CLAUDE.md sí se versiona)
.claude/settings.local.json

# loadtest artifacts
scripts/loadtest/monitor/run_*/
scripts/loadtest/monitor/current
scripts/loadtest/results/

# corpus de eval de STT (lo genera `make stt-corpus`)
scripts/stt_corpus/data/

# archivos temporales de trabajo (muestras, scripts sueltos, descargas)
scratch/

# fine-tuning de TTS: datos, checkpoints y audios generados (docs/TTS_FINETUNE.md)
tts/finetune/work/
