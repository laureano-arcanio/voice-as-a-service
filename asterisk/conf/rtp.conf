; Rango RTP: tiene que coincidir con el port forwarding UDP del router (ver
; docs/TELEFONIA_ANURA.md). Cada llamada usa 2 patas (Anura + LiveKit) x 2
; puertos (RTP + RTCP): 200 puertos alcanzan para ~50 llamadas simultaneas,
; de sobra para el limite de 10 de la troncal de Anura.
[general]
rtpstart=${ASTERISK_RTP_START}
rtpend=${ASTERISK_RTP_END}
